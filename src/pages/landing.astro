---
import '../utility/reset.css';
import '../utility/styles/main-styles.css';
import '../utility/styles/form-styles.css';
import '../utility/styles/root-variables.css';

import viewport from '../utility/image/viewport4.png';

import Image from 'astro/components/Image.astro';
import LandingLayout from '../layouts/LandingLayout.astro';
---

    <LandingLayout>

        <div class="container">
            <div class="image">
                <Image
                    src={viewport}
                    alt=""
                />
            </div>
            <h1>THANK GOD IT'S TUESDAY</h1>
            <!-- <p>my work contains transgressive adult themes intended for mature audiences only, including but not limited to non-con/dub-con, csa/cocsa, major age gaps and incest.</p> -->
            <p>my work contains trangressive themes in a fictional context that may be upsetting for some viewers and is intended for mature audiences only.</p>
            <p><span class="fa-solid fa-ban" aria-label="ban symbol"></span> denotes adult or erotic content and <span class="fa-solid fa-feather" aria-label="feather-symbol"></span> denotes content for which the above warning applies.</p>
            <p><b>viewer discretion is advised.</b></p>
            <p>by submitting below you affirm that you are at least 18 years of age, aware of the content warnings provided and consent to viewing such materials.</p>
            <div class="verifyage-input">
                <input type="checkbox" id="verifyage-checkbox">
                <label for="verifyage"><b>i consent</b></label>
            </div>
            <button id="verifyage-button" type="button">SUBMIT</button>
            
                </div>
            </div>

            
        </LandingLayout>
        <div class="hit-counter"></div>
        <script is:inline>
            const hitCounter = document.querySelector(".hit-counter")
            function cb(response) {
                let previousValue = 1245;
                let apiCounts = previousValue + response.value;
                hitCounter.innerText = "000" + apiCounts;
            }
        </script>
        <script is:inline src="https://abacus.jasoncameron.dev/hit/rudytues.day/visits?callback=cb"></script>

<style>
    
    body {
        color: #fff;
        background-color: var(--primary-clr-900); 
        line-height: 1.6;
        padding: 1.5em;
        text-align: center;
    }

    h1 {
        margin: 1em auto;
        max-width: 15ch;
        color: var(--white-clr);
    }

    p {
        margin-block: 1.5em;
        text-wrap: pretty;
    }

    .hit-counter {
        font-family: var(--display-font);
        font-size: 1.125rem;
        line-height: 1;
    }

    .image {
        width: fit-content;
        margin-inline: auto;
    }
    
    .fa-solid {
        font-size: 0.9em; 
    }

    #counter {
        padding-block: 0.25em;
    }

</style>
</style>

    <script>

    const verifyageButton = document.getElementById("verifyage-button") as HTMLInputElement;
    const verifyageCheckbox = document.getElementById("verifyage-checkbox") as HTMLInputElement;
        
    verifyageButton?.addEventListener("click", () => {
        // let url: URL = new URL(window.location.href);
        if (verifyageCheckbox?.checked) {
            localStorage.setItem("ageVerified", "✓");
            if (window.location.search.startsWith("?returnto=")) {
                window.location.href = window.location.search.substring("?returnto=".length);
            } else {
                window.location.href = "/";
            }
        } else {
            alert("you must be 18 years or older to submit");
        }
    });

    </script>
